# FacialRecognition
The model is trained on a database of labeled images and learns to extract unique facial features. Once trained, it can compare two pictures of the same person and determine whether they match - even if one of the images contains partially occluded features, such as a protective mask.
